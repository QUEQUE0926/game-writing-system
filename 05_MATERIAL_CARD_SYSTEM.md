# 05_MATERIAL_CARD_SYSTEM

## 定义

Material Card = 可追溯、可复用的作者亲历素材资产。

## 建议结构

Observation → Experience → Possible Cause → Interpretation → Judgement。

## 建议字段
- subject
- observation
- experience
- possible_cause
- interpretation
- judgement
- quote
- evidence_strength
- writing_value
- author_interest
- reuse_value
- status

## 证据绑定

一张卡可绑定多个 Segment；不得只存自由文本引用。

## 评分原则

不使用单一总分决定命运；当前文章不用不等于长期无价值。

## 人工优先

AI 可建议，作者确认优先级高于模型评分。

## 提炼约定（2026-09-12 新增，针对创作痛点的预制写作零件）

> 依据：`I:\AIstore\8.Gamedata\扁鹊鹊创作痛点研究.md`。核心思路：
> 卡片不是"经历记录"，提炼时就预制成"写作零件"——标题的坯子、开头的料、
> 能独立成段的支线模块。发散脑只负责发散，收束位外化给卡片结构。

### 字段写法约定（不动 Schema，只约定内容）

- **subject = 钩子坯**：15 字以内、带情绪的句子，不写档案标签。
  不写"镇邪Ⅱ开局捏人系统体验"，写"道袍一穿，我成了全村唯一的道士"。
  写作时标题 = 从钩子坯库里挑一个微调，不做从零压缩（痛点一）。
- **quote = 逐字原话**：存作者当时嘴里的原话，依赖 speaker 分类正确
  （区分作者/队友/游戏语音，防止把 NPC 台词错当亲历——事实错误与
  饭味风险的源头）。带高能原话的场景卡天然是开头素材（痛点二）。
- **没有 judgement 的卡不收**：interpretation → judgement 两段是强制
  收束机制，落到"这说明什么/我的判断"才算合格。写文章时插支线 =
  插一张卡，judgement 就是兜回主线的那句话——闭环从脑内负担变成
  查账动作（痛点三）。
- **evidence_strength 高分必须有原话锚点**：保证引用时不用回原文翻找
  核对，省改稿工程（痛点五）。

### 提炼流程约定

1. 一集（Episode）出 1~3 张卡，不贪多：只挑作者情绪最高的瞬间
   （骂了、笑了、惊了），低能片段不入库。
2. AI 只负责提名候选瞬间（哪几段情绪密度高），作者挑选和改写——
   AI 当灵感库不当代笔。
3. 四维评分入库时随手打；写作时按分检索：
   - writing_value 高 → 近期可用池；
   - reuse_value 高 → 跨游戏通用池。

### 对应痛点地图

| 痛点 | 卡片机制 |
|---|---|
| 标题排在精力最低点 | subject 存钩子坯，尾段只挑不改 |
| 开头卡死 | quote 存高能原话，开头有现成第一句 |
| 支线兜不回 | judgement 强制收束，插卡即插模块 |
| 收尾工程塌方 | 评分检索替代记忆翻找 |

## 落地实现（2026-09-12 与代码同步）

> 管线三件套已落地并跑通《灰烬之国》全流程（33 卡）。本节记录与代码的
> 实际对应关系，改代码须同步改本节。

### 管线与脚本

1. **筛窗** `scripts/screen_windows.py`：8 类信号词规则召回 + 窗口合并
   （±20 句、近距合并、不跨集），双分制（总分≥3 入围、密度分排序）。
   产出每游戏+每版本一份工作台 + `windows-<日期>.jsonl`。
2. **粗判门控** `scripts/coarse_gate.py`：本地 qwen3:4B 主判（JSON schema
   强制 KEEP/MAYBE/DROP），deepseek-r1 只复议"密度前30%但没给KEEP"的
   争议件。判定缓存 `gate-state.json` 断点续跑。产出精读名单
   `readlist-<日期>.jsonl` + 每游戏+每版本粗判工作台。
3. **精读产卡** `scripts/craft_cards.py`：读精读名单出卡。现阶段强模型
   由会话 AI 直读窗口手写卡，走 `--from-json` 模式（卡源 JSON 如
   `data/dev/tmp/my_cards.json`），将来接联网强模型只改
   GWS_LLM_URL / GWS_LLM_MODEL 环境变量。
4. **写库** `scripts/import_cards.py`（2026-09-13 新增）：把
   `cards-state-<日期>.json` 导入 material_cards + material_card_evidence
   （+ audit_log）。默认 dry-run，`--apply` 才落库。字段映射固定
   （detail→observation、feeling→experience、analysis→interpretation、
   judge_reason→judgement、concrete→evidence_strength、writing→
   writing_value、help→author_interest、unique→reuse_value，映射记入
   audit_log）；证据绑定=原话去空白后在窗口行区间内的 segments 逐字命中，
   命中 0 条拒绝入库；同 episode+同 quote 幂等跳过。前置条件=卡片草稿
   人工审核通过、human_check 回填归档。

### 程序锁（模型无权填的字段）

- **原话逐字锁**：quote 去空白后必须在窗口原文逐字命中；不过→带错误
  重试一次→再不过进 human_check，绝不静默入库。对 AI 手写卡同样生效。
- **证据位置**：程序从窗口锚点带出（游戏/版本/集/行号），模型无权填。
- 同窗 quote 去重；渲染前应先自查（norm+find），别浪费重试。

### 报告版式规矩

- 一律按游戏+版本一份文件（草稿、human_check、筛窗台、粗判台），
  文件头有「系列｜游戏｜版本」门牌行（系列从库查，无系列显示 —）。
- **报告只留最新**：三个产线脚本写完自动清理同模式的旧日期文件
  （`gws.report_prune.prune_dated_reports`）；jsonl/gate-state 等机器
  中间件不清理（断点续跑和跨日流程要用）。
- 卡头：基础价值/分项（"名称 **分数**"竖线式，最高分加粗）/建议用途/
  联网策略/判断理由/机会价值；「AI辅助结论（联网）」在折叠证据区内。
- 高/中/低三档分组，组内总分降序（同分按玩家帮助→判断增量→独特性）；
  卡号=稳定 ID，重排只动位置不重编号。
- 自我反转卡渲染 `- 反转:参见素材卡 NN` 互引行。

### 跨版本双向引用（算法自动匹配）

`auto_link_cross_version()` 两个信号，命中即双向挂「相关联卡」行：
- **孪生窗口**：两版本窗口原文 bigram 相似 ≥0.60（同一段内容被重复
  转写/两份录像）；
- **稀有主题词**：两卡主题字段共享纯汉字二元组、该词本批出现 ≤3 卡、
  共享 ≥1 个。
配对依据打印在运行日志。全文本相似度/详情词共现两版算法因太哑/太滥
弃用，勿回退。写卡人可用 `related` 字段手工补充算法够不着的引用。

### 联网规矩（2026-09-13 大修订：事实核验与社区佐证分轨）

> 用户拍板：旧规矩把"事实核验"和"社区扫描"混在一谈，社区帖被当成核验
> 依据。新规矩把两条轨道拆开，社区平台可作核验佐证但必须标可信度等级。

#### 轨道一：事实核验（fact_check）

回答"这条事实对不对"。**不受价值档限制**——高/中/低卡，只要出现以下
5 类核验对象之一就必须核：

1. 作品/产品名：官方中文名、英文名、别名、译名、易错写名；
2. 专有名词：机制名、地名、角色名、机构名、术语；
3. 版本号/版本状态：EA/正式版、更新版本、补丁号；
4. 开发方/发行方/所属机构；
5. 疑似转写错字：谐音、OCR 误识、口误造成的错写。

**不核**：作品内部虚构设定（虚构机构名、剧情专有词）不外部对照，
按素材原样保留并标"虚构设定，不外部对照"。

**源优先级**（权威一手优先，前一层能满足就不继续）：
① 官方商店页 → ② 官方公告/更新日志/patch notes → ③ 开发方/发行方
官网 → ④ 会议演讲/当事人访谈原始稿 → ⑤ 论文/书籍/官方文档 →
⑥ 可靠媒体原始访谈（非通稿转载）。
聚合转载、营销通稿、定位不到原出处的二手说法只当线索，不作最终依据。

**时效**：发售日、价格、版本、补丁、已确认问题等易变信息必须在核验
当日重新联网，不得用旧缓存当事实。

**红线**：不绕过验证码/登录墙/平台安全策略；遇 403/412/验证码/动态
空页，记录限制并降级"无法验证"，不反复高频重试。

#### 轨道二：社区佐证与社区扫描（community_scan）

- **社区佐证**（核验用途）：官方源缺失/难定位时先探路、交叉验证官方
  口径、确认社区通用叫法与卡内写法是否一致。社区平台不是权威事实源，
  判定必须带等级措辞（见下）。
- **社区扫描**（机会价值）：回答"大家在讨论什么/有没有流派参照"。
  按价值档调度：**高+中价值卡都做**（2026-09-13 用户拍板扩大到中），
  低价值不做。
- **平台清单（14 个，用户拍板）**：B站、其乐 Keylol、小黑盒、小红书、
  微博、贴吧、知乎、机核、TapTap、好游快爆、3DM、游民星空、
  Steam社区、Reddit。来源写具体页面（站名+标题+链接），
  禁止"知乎/TapTap攻略"式模糊合集。

**社区佐证判定等级（措辞冻结，写进「核对（联网）」）**：

| 等级 | 条件 | 措辞 |
|---|---|---|
| 强证实 | 平台出现官方账号/开发者/发行商原帖 | 已核实（社区官方源佐证）+出处 |
| 弱证实 | ≥2 独立非官方账号一致，无反例 | 社区多源一致佐证（非官方）+出处；标注"待官方源确认" |
| 反证 | 社区主流写法/版本与卡内不一致 | 存疑：社区通用写法为X与卡内Y不一致 → 转 human_check |
| 无信号 | 各平台均未命中 | 社区侧无信号 → 转官方源核验/human_check，**不写"没有"** |
| 无法验证 | 验证码/412/预算耗尽 | 无法验证（社区侧），不写"没有" |

**铁律**：弱证实/无信号/无法验证不能单独作事实结论，必须回官方源或
转 human_check；反证必须转 human_check，AI 不许自行改卡内写法。

#### 小黑盒专属取数链路（兜底，用户拍板 2026-09-13）

小黑盒匿名搜索不可用（需登录），唯一正确路径=**社区 id → 话题流**：
1. **零联网**连 `I:\AIstore\13.xhhcatch\xiaoheihe_anonymous_ai_project_docs\data\xhh.db`，
   `SELECT topic_id, name FROM topic_catalog WHERE name LIKE '%游戏名%'`
   取 topic_id；**不联网找 id、不猜 id**，命中 0 条或多条 → 停手登记
   human_check，由用户登记后再跑。
2. 按 `TECH_PLAN_xhh_community_fetch.md` 抓该 topic 的话题流（帖子天然
   强相关，社区边界即第一道关）。
3. 命中内容按统一三件套输出（见下）。灰烬之国 topic_id=665862 已实测通。
执行手册：同目录 `PLAYBOOK_xhh_executor.md`。

#### 核验输出三件套（每条核验/佐证强制）

每条结论必须带：**① 结论（含等级措辞与可信度说明）② 具体链接
③ 从来源页逐字摘取的原文片段**。片段摘不出来的来源不许列。
易变信息标注核验日期。

#### 数据与渲染落地

- 卡源 JSON（`my_cards.json`）联网字段拆两类：`fact_check`（逐条：
  对象/等级/结论/链接/原文片段/核验日期）与 `community_scan`
  （同结构）；旧 `web_check`/`ai_web` 自由文本字段废弃。
- 渲染：素材卡与 human_check 分栏显示「核对（事实核验）」「社区参照」；
  反证/无信号/无法验证项自动进 human_check 待人工清单。
- 「联网策略」仍只用四值枚举：重点联网/条件联网/顺带核对/不联网。

### human_check 规矩

- 按游戏+版本拆分，与草稿文件一一对应；每轮全量重写。
- 每条：窗口 → 主题线索 → **卡内原话** → 待人工核对点 →
  **原文上下文**（原话所在句±2句，带原文行号，按版本精确取源）→
  AI辅助结论（联网）→ 人工结论（人填）。
- 末尾「提交状态：（待填写）」+ 完成标准（条数按实际生成）。
  **归档前机器验证**：文件内「（待填写）」为 0 处且提交状态=已填写，
  不通过不改名归档。

### 特殊卡型

- **版本对比卡**（用户明确要求）：从转录里挖跨版本对比语句
  （"XX好像被削了""新地图感受不到"）单独立卡，张力常为反预期。
  同一游戏多版本时铺量必查。
- **跨游戏对比/类比卡**（2026-09-12 用户拍板）：玩家实况中提到其他
  游戏（"这像XX""比XX好"）的瞬间要立卡，并把提及的游戏名登记进
  game_aliases 花名册（对不上号的进 human_check）。

### 跨游戏关联（2026-09-12 拍板，2026-09-13 补充定案）

三个信号源，按确定性从高到低。**关联对象只限本地库已有的游戏**；
卡与卡的关系线记入 cross_reference 表，**素材卡阶段提到就算，
不要求正文用过**。

1. **玩家明说**：精读出卡时 AI 登记提及的游戏名 → 经 game_aliases
   解析 → 命中本地另一游戏的素材卡即当场挂 cross_reference
   （similar_to / contrasts 等，关系表已建、写库代码待落地）。
   - **本地没有的游戏**（如玩家随口类比的外部游戏）：不建档案、
     不进 game_aliases，只把提及名老实记在卡字段 + human_check
     清单上。这不丢数据——见下面的补线机制。
   - **补线机制**：关联匹配每次跑都是扫全库卡对。将来那个外部
     游戏真进了库，导入时把旧提报名登记为它的别名（game_aliases
     本职），重跑一轮匹配，老卡与新卡的线自动补上，与当场画出的
     线完全等价，老卡无需手工回改。
2. **内容相似**（玩家没说也查）：类型/机制接近的游戏，素材卡之间
   做语义相似匹配。实现候选=本地 qwen3-embedding。**复测时机=
   第二个游戏产卡之后**（此前判"分不开高低"是因没有真卡可比）；
   复测设计：灰烬两版本的孪生印证卡当正样本、随机跨游戏配对当
   负样本，看能否拉开；分不开再考虑联网嵌入或人工规则。
3. **联网联想**（用户拍板：**提前到素材卡阶段**，修改原"价值拓展型
   留写作期"的规矩）：出卡时按游戏（非逐卡）做一轮同类游戏联想，
   找社区对比讨论作为关联候选，登记出处；成本可控优先级排在 1、2
   之后，且遵守上方联网来源规矩。

**落地顺序（2026-09-13 定）**：前提=用户验收首批 33 张灰烬卡 →
素材卡写库迁移（新编号迁移文件，不动结构）→ 两个新游戏产卡
（产卡即带信号 1）→ embedding 复测（信号 2）→ 联网联想（信号 3，
随联网模型接入 GWS_LLM_URL 换强模型一起上）。匹配实现复用现有
配对框架，去掉"同游戏"过滤并加跨游戏专属信号。
